id: 6e12ebfe-945b-485f-8fab-fb675b45abe8
status: test
description: 'Detects the execution of certutil.exe with the "decode" or "encode"
  flag.

  This utility can be used to decode or encode files, which can be abused to execute
  malicious content.

  This rule looks for the execution of certutil.exe with the "decode" or "encode"
  flag and a file as an argument.

  '
author: AutoGenerated
date: '2025-07-12'
modified: 2023-02-09
tags:
- attack.defense-evasion
- attack.t1140
logsource:
  category: process_creation
  product: windows
detection:
  selection_img:
  - Image|endswith: \certutil.exe
  - OriginalFileName: CertUtil.exe
  selection_cli:
    CommandLine|contains:
    - ' decode '
    - ' encode '
  condition: all of selection_*
falsepositives:
- Unknown
level: medium
title: '[AUTO] Detects the execution of certutil.exe with the "decode" or "encode"
  flag.

  This utility can be used to decode or encode files, which can be abused to execute
  malicious content.

  This rule looks for the execution of certutil.exe with the "decode" or "encode"
  flag and a file as an argument.'
